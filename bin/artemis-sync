#!/bin/bash
# Wrapper for Artemis contacts sync tool.
# Available on PATH when the artemis plugin is enabled.
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
cd "$PLUGIN_ROOT" && exec uv run python tools/sync_contacts.py "$@"
