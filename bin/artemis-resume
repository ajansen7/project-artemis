#!/bin/bash
# Wrapper for Artemis resume generator.
# Available on PATH when the artemis plugin is enabled.
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
cd "$PLUGIN_ROOT" && exec uv run python tools/generate_resume_docx.py "$@"
